## Test environments
 * local: macOS 26.5.2, R 4.6.1 (release)
 * win-builder: R-devel and R-release
 * GitHub Actions: ubuntu-latest (release, devel, oldrel-1), macOS-latest, windows-latest

## R CMD check results

0 errors | 0 warnings | 1 note

 * This is a new release. 
 * 2 URLs are pointing to CRAN would be URLs if the package is accepted, and thus currently error 404
 * a few last names and truncated words (because of accents or markdown mark-ups) appear as false positives when spell-checking the package